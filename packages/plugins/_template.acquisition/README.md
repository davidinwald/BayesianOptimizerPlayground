# Template Acquisition Plugin

This is a template for creating acquisition plugins. Copy this directory and modify it to create your own acquisition function.

## Required Methods

- `score(Xstar, posterior, context, parameters)` → Vector

## Example

See the EI (Expected Improvement) acquisition implementation for a complete example.

